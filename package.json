{
  "name": "yaml2xmlua",
  "description": "Yaml to Xml-Lua converter",
  "version": "0.1.2",
  "main": "dist/main.js",
  "bin": {
    "y2xl": "dist/bin.js"
  },
  "scripts": {
    "clean": "rimraf dist/",
    "build": "tsc --build",
    "watch": "tsc --watch",
    "rebuild": "npm run clean && npm run build",
    "rewatch": "npm run clean && npm run watch",
    "prepare": "npm run rebuild --silent"
  },
  "author": "NAYOTO.",
  "license": "MIT",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@types/js-yaml": "^4.0.5",
    "@xmldom/xmldom": "^0.8.3",
    "js-yaml": "^4.1.0",
    "rimraf": "^3.0.2"
  }
}
