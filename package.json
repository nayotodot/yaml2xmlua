{
  "name": "yaml2xmlua",
  "description": "Yaml to Xml-Lua converter",
  "version": "0.1.1",
  "main": "dist/main.js",
  "bin": {
    "y2xl": "bin/cli.js"
  },
  "scripts": {
    "clean": "rimraf dist/",
    "build": "tsc --build",
    "watch": "tsc --watch",
    "rebuild": "npm run clean && npm run build",
    "rewatch": "npm run clean && npm run watch"
  },
  "author": "NAYOTO.",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@types/js-yaml": "^4.0.5",
    "@types/node": "^18.11.0",
    "@xmldom/xmldom": "^0.8.3",
    "js-yaml": "^4.1.0",
    "rimraf": "^3.0.2"
  }
}
